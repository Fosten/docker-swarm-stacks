version: '3.9'

services:
  ollama:
    image: ollama/ollama:latest
    networks:
      - traefik-public
    volumes:
      - ${HOME}/ollama:/root/.ollama
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.labels.type == ${STACK_PARAM}

networks:
  traefik-public:
    external: true